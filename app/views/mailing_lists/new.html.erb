<div id="user_info_header">
        <div id="user_info_header_left">
                <h1>Add yourself to the mailing list:</h1>
        </div>
</div>
	<%= form_for(@mail) do |f| %>
	<%= f.label :email %>
	<%= f.text_field :email %>
	<%= f.submit "Submit" %>
	<% end %>
